<%= f.label "Comments:" %><br>
<div id="comments-previous"></div>
<div id="comments-new">
  <%= f.fields_for :comments do |ff| %>
    <%= ff.hidden_field :user_id %>
    <%= textarea_or_straight_text(ff) %>
  <% end %>
</div>